version: "3.7"

services:
    movies-api:
      build: 
        context: .
        dockerfile: DockerFile
      ports:
        - "5001:5001"
        - "3000:3000"
      volumes:
        - ../:/app
      environment:
        - TZ=UTC
      tty: true
